//! Chat Domain Command Handlers
//!
//! Tauri command handlers for chat functionality.
//! NOTE: These are prepared for migration but not yet active.
//! The active commands are still in services/chat.rs

// Commands will be enabled when we fully migrate from services/chat.rs
// For now, this module is a placeholder.

// When ready to migrate, uncomment and update imports:
/*
use tauri::State;
use super::models::{ChatApiResponse, ChatAttachment, ChatMessage};
use super::service::{call_ai_api, ChatState};
use crate::domain::settings::get_settings;

#[tauri::command]
pub async fn save_chat_message(...) { ... }
*/
